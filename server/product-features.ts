/** Release gates shared by the renderer and server. Disabled providers stay
 * visible in the UI, but no public API may configure or contact them. */
export const RUIJIE_SANDBOX_ENABLED = false;

export const RUIJIE_SANDBOX_UNAVAILABLE_MESSAGE = "Ruijie sandbox is temporarily unavailable";
