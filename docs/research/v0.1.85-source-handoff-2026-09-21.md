# 0.1.85 源码同步准备与打包问题复核

日期：2026-09-21。目标公司仓库：`AI-Applications-Team/OpenMausBot`。
本轮只准备源码、依赖和文档；停止安装器构建，不修改公司工作流，不触发 Release。

## 已确定的源码交付方式

公司 main：`aa7754725c81896c439fd261dee867703e4cf02e`。
原本地开发分支：`codex/chat-profile-artifacts`，HEAD `ecac4d337e414c0daffce7b409a4a70ad71c33c9`。
本地未提交改动的隔离快照：`2e65c571d4ca5800985559c5ddf0a71a9a514925`。

公司相对原本地 HEAD 独有 335 个提交，本地独有 14 个；两版完整目录差异达 1034 个文件。
只读 merge-tree 预演报告 48 处冲突，集中在运行时、会话/问题协议、界面、发布工具和指南。
用户已明确选择：保留本地经过判断的选择性升级，独立提交公司 `0.1.85` 分支及同名 tag，不修改 main。
最初的设计分析已经以 `aa775472` 为比较对象；335 是包括上游历史在内的图差异，不是新发现的335项遗漏。
逐项结论见 [选择性升级复核](v0.1.85-selective-upgrade-audit.md)。

## 已核实的历史失败

| 原始运行 | 最早的实质失败 | 对应准备 |
| --- | --- | --- |
| [35338906783](https://github.com/AI-Applications-Team/OpenMausBot/actions/runs/35338906783) | EMFILE，打开文件句柄耗尽；日志路径是正在读取的 js.map，并非缺失模块证据 | Mac 串行签名、thin sidecar；后续 YAML 补句柄限制 |
| [35347474791](https://github.com/AI-Applications-Team/OpenMausBot/actions/runs/35347474791) | Electron Framework 嵌套代码未签名 | Helpers/Libraries 先于框架/App；最终 DMG 挂载验签 |
| [35350741318](https://github.com/AI-Applications-Team/OpenMausBot/actions/runs/35350741318) | Missing or duplicate platform manifests | collector 同时识别单平台平铺与多平台子目录；重复/缺平台仍拒绝 |

以上三份原始失败日志本轮重新读取。历史恢复运行成功不等于当前 main 包含修复。
私有 Harness 检出凭据错误、生成图标造成的 dirty 检查冲突，分别由只读 Secret 接线和严格的已知生成文件白名单处理。
Secret 名称存在不证明实际权限；本次不读取或提交凭据。

## 必须一起提交的源码依赖

当前公司 main 缺少下列关键新增文件，不能只传 UI、四份指南或某一份 YAML：

- `shared/ruijie-harness-release.ts`：统一 Harness 2.1.10 固定提交。
- `scripts/build-ruijie-harness.mjs` 及测试：完整源码构建、锁元数据稳定化、已验缓存复用和 thin CPU 覆盖。
- `scripts/package-enterprise-macos-thin.mjs`：分架构 App 到 DMG，挂载后再验证。
- `scripts/enterprise-macos-sign.test.mjs`：嵌套签名顺序与固定资源保留的回归。
- `scripts/smoke-harness-bundle.mjs` 及测试：用包内 Electron，在空 home/cwd、空 NODE_PATH 下解析真实启动导出，拒绝借用开发 node_modules。
- `.release/repository.mjs` 及测试：明确公司/个人仓库身份。
- `.release/toolkit/test/tagged-release.test.mjs`：标签源码保持与预处理元数据验证。

同时带上 adapter、toolkit core/github/bin、enterprise builder、Harness staging/识别的修改及引用的已有模块。
不上传 node_modules、dist-native、安装包、个人诊断缓存或账号数据。
不把“新增检查脚本”夸大成“已证明所有懒加载依赖无缺失”；最终包仍须在原生平台执行 smoke。

## Mac 两包准备

共用 Bot 0.1.85 源码/版本、toolkit tree、Harness pin 与依赖锁文件。
arm64/x64 各自使用原生 runner、CPU 匹配的 Node/Electron/Harness/CUA/浏览器/飞书资源、独立缓存和暂存目录。
各自生成 DMG、哈希、签名审计、平台 manifest 和真人验收记录。
单独下载的 Harness Universal 发行不变。本机 Windows 结果不能替代 Mac 实测。

## 当前检查结果与未完成项

- 固定 Node 24.20.0 / pnpm 10.33.0，frozen-lockfile 安装通过；0.1.85 源码 typecheck 通过。
- 无安装器 Node 检查：35 通过、1 失败；Vitest 检查：42 通过、1 失败。
- 两项失败均为公司旧 YAML 未检出/准备固定 Harness；保持断言，列为后续工作流任务，不伪造全绿。
- 本地预览此前已通过输入栏间距、加载布局、图片/文件预览等隔离 Electron 回归；Harness 2.1.10 已通过 Windows 全量内容校验。
- 0.1.85 未生成安装器、未执行 Mac 原生构建/安装/权限验收；四份指南已经明确这些边界。
- 公司 main 和 Release 保持不变；本轮只创建 `0.1.85` 分支及同名源码 tag。

用户要求的 tag 为 `0.1.85`，不是 `v0.1.85`；已检查现有 `v*` 发布规则不匹配本次 tag。
main push 自带 CI/Docs/Docker 检查；它不等于手动授权生成桌面安装器。
